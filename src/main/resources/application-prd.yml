spring:
  datasource:
    url: jdbc:postgresql://gondola.proxy.rlwy.net:53583/railway
    username: postgres
    password: proLkQjBlbcvpELGdJQMAzyiBtrgSqRJ
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    format-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
